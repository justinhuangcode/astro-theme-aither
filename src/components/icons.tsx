/**
 * Unified icon re-exports from geist-icons.
 *
 * All icons are tree-shakeable — only imported icons are bundled.
 * Works in both React components and Astro components (rendered as static HTML).
 * Source: https://www.npmjs.com/package/geist-icons
 */

// Navigation & UI
export {
  Sun as IconSun,
  Moon as IconMoon,
  DesktopDevice as IconMonitor,
  Menu as IconMenu,
  CrossSmall as IconX,
  Globe as IconLanguage,
  ChevronRight as IconChevronRight,
  ChevronDown as IconChevronDown,
  Layers as IconPalette,
  Cross as IconClose,
} from 'geist-icons';

// Arrows
export {
  ArrowLeft as IconArrowLeft,
  ArrowRight as IconArrowRight,
} from 'geist-icons';

// Content
export {
  Pin as IconPin,
  Link as IconLink,
  Message as IconMessage,
} from 'geist-icons';

// Social
export {
  LogoGithub as IconGithub,
  LogoTwitterX as IconTwitterX,
  LogoDiscord as IconDiscord,
  Envelope as IconMail,
  Rss as IconRss,
} from 'geist-icons';
